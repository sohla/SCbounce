liveSession/mball1 Project/Samples/*
.DS_Store
/embed/M5bounceX/.DS_Store
/embed/WemosBN0055/.DS_Store
/data
/out

# Generated analysis data. Regenerate with analysis/update.py - the scripts
# are the source of truth, not the JSON they emit.
/analysis/output/
/analysis/__pycache__/

*.png
*.wav

# Optional per-machine override naming which machines/*.scd to load. The
# machine files themselves are tracked; this pointer is not, because it is
# the one filename every kit would edit.
/machines/CURRENT
