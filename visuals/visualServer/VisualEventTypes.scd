// NO LONGER REQUIRED.
//
// The visual event types (\visual \vpulse \vline \vlive \audioVisual) are now
// auto-registered at class-library compile time by VisualEventTypes:*initClass
// (Classes/VisualEventTypes.sc). You do NOT need to load this file.
//
// This stub is kept only so older scripts that still `.load` it do not error.
// It intentionally does NOT register anything (the class is the single source
// of truth).

"VisualServer: event types are auto-registered at class compile - this file is no longer required.".postln;
